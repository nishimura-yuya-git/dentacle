# 削除手順（運営向け）

院向けの正は `/security/privacy#retention`。画面からの自己削除はない。

1. 医院管理者または運営が、対象クリニックと削除範囲を確認する。
2. 患者種まき・予約・所属など、依頼された業務データを消す。
3. 取込CSVそのものは取込後に残していないので、追加削除はない。
4. 操作ログとログイン監査は件数中心。保存年数は5年。改ざん防止のため運営のみが扱う。
5. 完了を依頼者へ報告する。

種まきは利用中保持し、契約終了または削除指示から90日以内に消す。
診療録の法定保存と混ぜない。
