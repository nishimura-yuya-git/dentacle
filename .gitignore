# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
.env.local
.env.*.local
.env.development.local
.env.test.local
.env.production.local
!.env.example

# Hard Boundary 一時許可（セッション限定。コミット禁止）
.cursor/hard-boundary-session-allow.json

# 変更契約ゲート（セッション限定。コミット禁止）
.cursor/change-contract-gate.json

# ループ状態・MEMORY追記候補（自動生成）
state/

# 隔離用 git worktree
.worktrees/

# Apotool 一時スクレイプ成果物（個人情報）
tmp/

# 手元調査・実データ（Git に載せない。開発用にローカルへ置く）
doc/患者データ.csv
doc/Apotool管理ツール調査結果_訪問歯科スケジュール自動化.md

# DB ダンプ
backups/
*.tsbuildinfo

# 調査用の一時スクリプト（個人情報・eval を含みうる）
.tmp-*.js
