# 変更履歴

製品版（Git タグ `vX.Y.Z`）の開発者向け記録です。
院向けお知らせの `update #N` や、DB の楽観ロック `version` 列とは別です。
院への掲載は `/announcements` の入れる判断が正です。

## 未公開

## 0.3.0 - 2026-08-25

### 追加

- 医院ごとの訪問メニュー設定を入れる（一覧・編集・削除。初回は既定メニューをコピー）
- 患者詳細から確定枠を登録できるようにする
- 希望曜日に、曜日ごとの希望時間・いけない時間・終日いけないを持たせる
- 患者の感染症フラグを一覧・電話確認一覧・訪問ブロックに出す
- MFA確認コードを6桁入力で自動送信する
- 認証画面のロゴ配置を画面ごとに分ける

### 変更

- 自動提案の重なり判定と空き帯の算出を1か所に集約する
- 訪問メニューをカレンダーの作成・詳細・ブリーフィングで共通化する
- 例外・NGの追加導線を希望曜日の見出し右へ移す
- 認証画面の案内を端末非依存にし、認証途中でもログアウトできるようにする
- 不具合修正の完成条件に症状ゲートを入れる（症状の再掲・前後の数値・同一環境・実差分を必須にする）

### 修正

- 終日いけないのクリックで画面が大きくスクロールする問題を直す
- 終日いけないのクリックで行が縮み、文字が上へ動く問題を直す
- チェックボックスの連打でラベル文字が範囲選択される問題を直す
- TypeScriptパス設定の非推奨エラーを直す
- 本番の直リンク404を直す

### 削除

- 検証用スクリーンショットをリポジトリから外す（実患者名が写るため）

## 0.2.0 - 2026-08-21

### 追加

- マイページで表示名を本人が編集できるようにする
- 患者アイコンと検索配置を一覧・電話確認・詳細で揃える
- 運営の認証アプリ初回登録に、iPhone 以外でも分かる最短手順を出す
- 診療カレンダーの他端末同期を入れる
- レセコン連携の身元受け皿と接続の安全条件を固定する
- 監査の保持期間（5年・90日・24時間）と出せる範囲を固定する

### 変更

- ご意見チャットの公開エラーと権限をログイン並みに固定する
- 運営の危険操作を追加確認（AAL2）必須にする
- 製品版の公開手順を `/release` に分ける

### 削除

- 実患者CSVと Apotool 調査メモをリポジトリから外す

## 0.1.0 - 2026-08-16

### 追加

- 製品版番号の運用を開始した（`package.json` / `APP_VERSION` / Git タグ / GitHub Releases）
- 現行の訪問スケジュール自動化一式を、最初の版 `v0.1.0` として固定する
